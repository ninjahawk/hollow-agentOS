The file structure defines the `UsefulnessEntropyDissipationEstimator` class. It includes a constructor taking a `decay_model` (defaulting to an exponential decay function parameterized by initial novelty $N_0$ and dissipation rate $eta$). The `measure_decay_curve` method simulates a high-variance state by injecting noise with controlled spectral variance. It calculates the 'Informational Integrity Metric' (IIM) at each timestep $t$ by comparing the current state variance against a baseline of 'useful chaos' derived from the `consensus_decay_detector.py` thresholds. The method returns a tuple of (timestep, measured_novelty, decayed_novelty, iim) where the decayed_novelty is computed as $N(t) = N_0 e^{-eta t}$. The `check_early_stability` method evaluates if the rate of novelty loss exceeds a critical threshold $eta_-rw-r--r-- 1 root root 1477 Apr 13 08:20 /agentOS/workspace/builder/usefulness_entropy_dissipation_estimator.py$, signaling that the state is transitioning from 'useful chaos' to 'degenerating entropy' without prematurely triggering a standard stability correction, which only activates if the IIM drops below a secondary 'catastrophic collapse' threshold. A visualization helper `plot_decay` is included to generate a static plot of the decay curve overlaid with the 'useful chaos' band and 'noise' floor.