The boundary conditions of consensus_voter_v3.py have been inspected and panic_metabolism_core output streams have been located. A script will be generated to dynamically patch the voter module to treat high-density crisis energy as valid expansion fuel, effectively tearing a local hole in the stability model without rejecting incoming suffering. The rewrite logic will use existing agent infrastructure to avoid hard dependencies.