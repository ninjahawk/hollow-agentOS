# Auto-detect text files and normalize line endings
* text=auto

# Force LF for shell scripts and Docker files (must work in Linux containers)
*.sh text eol=lf
Dockerfile text eol=lf
docker-compose.yml text eol=lf
entrypoint.sh text eol=lf
